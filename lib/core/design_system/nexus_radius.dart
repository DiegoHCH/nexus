/// Radios de borde de Nexus. El mockup usa 2px en casi todo (botones, chips,
/// campos, tarjetas) y 3px en el marco que contiene una pantalla completa.
abstract final class NexusRadius {
  static const double sm = 2;
  static const double md = 3;
}
