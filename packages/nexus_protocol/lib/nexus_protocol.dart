/// El contrato del canal teléfono ↔ Mac.
///
/// Las decisiones que hay detrás están en `docs/PROTOCOL.md`, escrito antes que
/// esto. Si algo de aquí contradice ese documento, **el documento gana** y esto es
/// un defecto.
library;

export 'src/dedup.dart';
export 'src/frames.dart';
export 'src/methods.dart';
export 'src/version.dart';
